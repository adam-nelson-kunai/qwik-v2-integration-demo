version: 3

tasks:
  build-v1:
    dir: ./project-v1
    cmds:
      - npm run build
  build-v2:
    dir: ./project-v2
    cmds:
      - npm run build
  diff-builds:
    deps: [build-v1, build-v2]
    cmds:
      - diff -bur ./project-v1/dist ./project-v2/dist
